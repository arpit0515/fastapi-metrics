[build-system]
requires = ["setuptools>=65.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "fastapi-metrics"
version = "0.4.0"
description = "Zero-config metrics for FastAPI apps - perfect for solo devs and MVPs"
readme = "README.md"
requires-python = ">=3.8"
license = {text = "MIT"}
authors = [
    {name = "Arpit", email = "arpit.thukral@gmail.com"}
]
keywords = ["fastapi", "metrics", "observability", "sqlite", "monitoring", "redis", "prometheus", "kubernetes"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Framework :: FastAPI",
]

dependencies = [
    "fastapi>=0.100.0",
    "pydantic>=2.0.0",
    "aiosqlite>=0.19.0",
    "psutil>=5.9.0",
]

[project.optional-dependencies]
redis = ["redis>=4.0.0"]
postgres = ["asyncpg>=0.27.0"]
dynamodb = ["aioboto3>=11.0.0"]
alerts = ["httpx>=0.24.0"]
cli = ["rich>=13.0.0", "uvicorn>=0.22.0"]
mcp = ["mcp>=1.0.0", "httpx>=0.24.0"]
all = [
    "redis>=4.0.0",
    "asyncpg>=0.27.0",
    "aioboto3>=11.0.0",
    "httpx>=0.24.0",
    "rich>=13.0.0",
    "uvicorn>=0.22.0",
    "mcp>=1.0.0",
]
dev = [
    "fastapi-metrics[all]",
    "pytest>=7.4.0",
    "pytest-asyncio>=0.21.0",
    "httpx>=0.24.0",
    "black>=23.0.0",
    "ruff>=0.0.280",
]

[project.scripts]
fastapi-metrics = "fastapi_metrics.cli_query:main"
fastapi-metrics-setup = "fastapi_metrics.cli:run_cli"
fastapi-metrics-mcp = "fastapi_metrics.mcp_server:main"

[project.urls]
Homepage = "https://github.com/arpit0515/fastapi-metrics"
Repository = "https://github.com/arpit0515/fastapi-metrics"
Issues = "https://github.com/arpit0515/fastapi-metrics/issues"

[tool.setuptools.packages.find]
where = ["."]
include = ["fastapi_metrics*"]

[tool.black]
line-length = 100
target-version = ['py38']
extend-exclude = '/\._'

[tool.ruff]
line-length = 100
target-version = "py38"
extend-exclude = ["._*"]
